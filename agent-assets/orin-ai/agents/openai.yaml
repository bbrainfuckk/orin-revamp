interface:
  display_name: "ORIN AI"
  short_description: "Operate a live ORIN AI workspace through its official CLI, MCP server, and API."
  default_prompt: "Use the loaded ORIN MCP tools or CLI. Read live workspace state, preserve provider delivery truth, confirm external publishing when needed, and never expose credentials."
